//Tests with chai
//As preparation only
import {Experimental} from "../../main/ts";

const tests = {
    chai : require('chai'),
    experimental : new Experimental(),

    runAllTests : function()
    {
    }
}