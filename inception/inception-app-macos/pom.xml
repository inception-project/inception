<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>de.tudarmstadt.ukp.inception.app</groupId>
    <artifactId>inception-app</artifactId>
    <version>41.0-SNAPSHOT</version>
    <relativePath>..</relativePath>
  </parent>

  <artifactId>inception-app-webapp-macos</artifactId>
  <name>INCEpTION - App - macOS application</name>
  <packaging>pom</packaging>

  <dependencies>
    <dependency>
      <groupId>de.tudarmstadt.ukp.inception.app</groupId>
      <artifactId>inception-app-webapp</artifactId>
      <version>${project.version}</version>
      <classifier>standalone</classifier>
    </dependency>
  </dependencies>

  <profiles>
    <profile>
      <activation>
        <os>
          <family>mac</family>
        </os>
        <property>
          <name>mac.signing.identity</name>
        </property>
      </activation>

      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <version>3.6.0</version>
            <executions>
              <execution>
                <id>stage-dependencies</id>
                <phase>prepare-package</phase>
                <goals>
                  <goal>copy-dependencies</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                  <includeScope>runtime</includeScope>
                  <overWriteReleases>false</overWriteReleases>
                  <overWriteSnapshots>true</overWriteSnapshots>
                  <overWriteIfNewer>true</overWriteIfNewer>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>3.1.0</version>
            <executions>
              <execution>
                <id>sign-native-libs</id>
                <phase>prepare-package</phase>
                <goals>
                  <goal>exec</goal>
                </goals>
                <configuration>
                  <executable>bash</executable>
                  <arguments>
                    <argument>${project.basedir}/src/main/scripts/sign-native-libs.sh</argument>
                    <argument>${project.build.directory}/jpackage-input</argument>
                    <argument>${mac.signing.identity}</argument>
                  </arguments>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <version>3.5.0</version>
            <executions>
              <execution>
                <id>parse-version</id>
                <phase>initialize</phase>
                <goals>
                  <goal>parse-version</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          
          <plugin>
            <groupId>org.panteleyev</groupId>
            <artifactId>jpackage-maven-plugin</artifactId>
            <version>1.7.4</version>
            <configuration>
              <name>INCEpTION</name>
              <appVersion>${parsedVersion.majorVersion}.${parsedVersion.minorVersion}</appVersion>
              <type>DMG</type>
              <input>${project.build.directory}/jpackage-input</input>
              <destination>${project.build.directory}/jpackage</destination>
              <mainJar>inception-app-webapp-${project.version}-standalone.jar</mainJar>
              <mainClass>de.tudarmstadt.ukp.inception.INCEpTION</mainClass>
              <javaOptions>
                <javaOption>-Xmx4g</javaOption>
              </javaOptions>
              <macSign>true</macSign>
              <macSigningKeyUserName>${mac.signing.identity}</macSigningKeyUserName>
              <macEntitlements>${project.basedir}/src/main/resources/entitlements.plist</macEntitlements>
            </configuration>
            <executions>
              <execution>
                <phase>package</phase>
                <goals>
                  <goal>jpackage</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
