kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: inception-tmp-pvc
spec:
  storageClassName: {{ .Values.storage.tmp.storageClassName }}
  accessModes:
    - {{ .Values.storage.tmp.accessMode }}
  resources:
    requests:
      storage: {{ .Values.storage.tmp.size }}